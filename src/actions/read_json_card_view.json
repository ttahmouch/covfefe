{
    "$method": "GET",
    "$uri": "/card.json",
    "$actions": [
        {"$action": "update_loading", "$value": true}
    ],
    "$responses": [
        {
            "$status": 200,
            "$headers": {"content-type": "application[/]json"},
            "$body": {"$schema": "http://json-schema.org/schema#"},
            "$actions": [
                {"$action": "update_loading", "$value": false}
            ]
        }
    ]
}